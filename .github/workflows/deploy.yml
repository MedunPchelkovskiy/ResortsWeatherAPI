name: Deploy FastAPI

on:
  push:
    branches: ["main"]

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Login ACR
        run: az acr login --name ${{ secrets.ACR_NAME }}

      - name: Build image
        run: docker build -t ${{ secrets.ACR_LOGIN_SERVER }}/fastapi:latest .

      - name: Push image
        run: docker push ${{ secrets.ACR_LOGIN_SERVER }}/fastapi:latest

      - name: Deploy
        run: |
          az webapp config container set \
            --name ${{ secrets.APP_NAME }} \
            --resource-group ${{ secrets.RG }} \
            --docker-custom-image-name ${{ secrets.ACR_LOGIN_SERVER }}/fastapi:latest \
            --docker-registry-server-url https://${{ secrets.ACR_LOGIN_SERVER }}